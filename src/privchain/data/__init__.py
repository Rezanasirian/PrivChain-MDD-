"""Data loading and synthetic-data generation for PrivChain-MDD.

The real DAIC-WOZ dataset is access-controlled and never committed (CLAUDE.md
§7). Tests and early development run against the mock dataset defined in
:mod:`privchain.data.mock_daic_woz`.
"""
